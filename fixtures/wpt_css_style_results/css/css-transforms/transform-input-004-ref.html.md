# css/css-transforms/transform-input-004-ref.html

```json
{
  "format_version": 3,
  "file": "css/css-transforms/transform-input-004-ref.html"
}
```

## style[0]

```css

      input {
        margin: 10px;
        position: relative;
      }
      p + input + input {
        left: -10px;
      }
      p + input + input + input {
        left: 10px;
      }
      p + input + input + input + input {
        top: -10px;
      }
      p + input + input + input + input + input {
        top: 10px;
      }
    
```

```json
{
  "errors": 0,
  "messages": [],
  "warnings": 0
}
```
