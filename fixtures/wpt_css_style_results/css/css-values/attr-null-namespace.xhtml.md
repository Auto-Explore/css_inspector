# css/css-values/attr-null-namespace.xhtml

```json
{
  "format_version": 3,
  "file": "css/css-values/attr-null-namespace.xhtml"
}
```

## style[0]

```css

        #target {
            --x: attr(data-x type(*), fallback);
            --y: attr(data-y type(*), fallback);
            --z: attr(data-z type(*), fallback);
            --w: attr(data-w type(*), fallback);
        }
    
```

```json
{
  "errors": 0,
  "messages": [],
  "warnings": 0
}
```
