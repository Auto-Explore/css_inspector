# css/css-overflow/auto-scrollbar-inline-children.html

```json
{
  "format_version": 3,
  "file": "css/css-overflow/auto-scrollbar-inline-children.html"
}
```

## style[0]

```css

  /* Avoid auto scrollbars on the viewport, because that might trigger re-layout
    (and thus hide bugs). */
  body { overflow:hidden; }

  .container { width:100px; }
```

```json
{
  "errors": 0,
  "messages": [],
  "warnings": 0
}
```
