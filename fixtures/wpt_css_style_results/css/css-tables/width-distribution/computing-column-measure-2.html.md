# css/css-tables/width-distribution/computing-column-measure-2.html

```json
{
  "format_version": 3,
  "file": "css/css-tables/width-distribution/computing-column-measure-2.html"
}
```

## style[0]

```css

td {
    padding: 0;
}
div {
    /* Bug does not happen when the table's containing block is less
       than (100+200+300)=600px, so make sure sure that it's larger. */
    width: 750px;
}
```

```json
{
  "errors": 0,
  "messages": [],
  "warnings": 0
}
```
