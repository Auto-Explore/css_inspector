# css/css-masking/mask-image/mask-image-cors-001.sub.html

```json
{
  "format_version": 3,
  "file": "css/css-masking/mask-image/mask-image-cors-001.sub.html"
}
```

## style[0]

```css

  /* Zero out the margin in the outer document, so that the iframe's inner
     document is rendered directly at the top-left corner. (This makes the
     reference case slightly simpler.) */
  body { margin: 0; }
  iframe {
    border: none;
    height: 400px;
    width: 400px;
  }
```

```json
{
  "errors": 0,
  "messages": [],
  "warnings": 0
}
```
